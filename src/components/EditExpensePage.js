import React from 'react';

const EditExpensePage = (props) => {
    console.log(props.match.params);
    return (
        <div>
            Editing page form the item with id
        </div>
    );
};

export default EditExpensePage;